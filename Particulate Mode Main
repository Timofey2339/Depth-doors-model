if not game:IsLoaded() then game.Loaded:Wait() end

local ReplicatedStorage = game:GetService("ReplicatedStorage")
local GameData = ReplicatedStorage:WaitForChild("GameData", 15)
if not GameData then return end

local LatestRoom = GameData:WaitForChild("LatestRoom", 15)
if not LatestRoom then return end

local achievementGiver = nil
pcall(function()
    achievementGiver = loadstring(game:HttpGet("https://raw.githubusercontent.com/Guestly-Alt/Scripts/refs/heads/main/AchievementHolder.lua"))()
end)

local function safeLoad(url)
    task.spawn(function()
        pcall(function()
            loadstring(game:HttpGet(url))()
        end)
    end)
end

task.spawn(function()
    while true do
        task.wait(55)
        safeLoad("https://raw.githubusercontent.com/Timofey2339/Depth-doors-model/main/Drave.lua")
        task.delay(1.5, function()
            if achievementGiver then
                pcall(function()
                    achievementGiver({
                        Title = "Stop locking at me",
                        Description = "Ouch",
                        Reason = "Meet the Drave",
                        Image = "https://raw.githubusercontent.com/Timofey2339/Depth-doors-model/main/Badges/DraveBadge.png"
                    })
                end)
            end
        end)
    end
end)

local startTime = tick()
local lastSpawnTimes = {
    Watcher = startTime,
    Spread = startTime,
    Ankle = startTime,
    NightmareApex = startTime,
    Greg = startTime,
    Feast = startTime,
    Glacher = startTime,
    A179 = startTime,
    Rebound = startTime,
    Anxiety = startTime,
    Dread = startTime,
    Monoxide = startTime,
    Rambey = startTime,
    Spark = startTime,
    Wide = startTime,
    A60 = startTime,
    RipperPrime = startTime,
    idk = startTime
}

local isSpawning = false

LatestRoom.Changed:Connect(function(roomValue)
    if isSpawning then return end
    isSpawning = true

    local currentTime = tick()

    if currentTime - lastSpawnTimes.Watcher >= 100 then
        lastSpawnTimes.Watcher = currentTime
        safeLoad("https://raw.githubusercontent.com/Timofey2339/Depth-doors-model/main/Watcher.lua")
        task.delay(1, function()
            if achievementGiver then
                pcall(function()
                    achievementGiver({
                        Title = "Look at me!",
                        Description = "I looking at you",
                        Reason = "Meet the Watcher",
                        Image = "https://raw.githubusercontent.com/Timofey2339/Depth-doors-model/main/Badges/WatcherBadge.png"
                    })
                end)
            end
        end)
        task.wait(5)
    end

    if currentTime - lastSpawnTimes.idk >= 150 then
        lastSpawnTimes.idk = currentTime
        safeLoad("https://raw.githubusercontent.com/Timofey2339/Depth-doors-model/refs/heads/main/%CE%A9.lua")
    end

    if currentTime - lastSpawnTimes.Ankle >= 285 then
        lastSpawnTimes.Ankle = currentTime
        safeLoad("https://raw.githubusercontent.com/Timofey2339/Depth-doors-model/main/Ankle.lua")
        task.delay(1, function()
            if achievementGiver then
                pcall(function()
                    achievementGiver({
                        Title = "its your time",
                        Description = "time didnt wait",
                        Reason = "Meet the Ankle",
                        Image = "https://raw.githubusercontent.com/Timofey2339/Depth-doors-model/main/Badges/DraveBadge.png"
                    })
                end)
            end
        end)
        task.wait(5)
    end


    if currentTime - lastSpawnTimes.NightmareApex >= 320 then
        lastSpawnTimes.NightmareApex = currentTime
        safeLoad("https://raw.githubusercontent.com/Timofey2339/Depth-doors-model/main/Nightmare%20Apex")
        task.delay(10, function()
            if achievementGiver then
                pcall(function()
                    achievementGiver({
                        Title = "Im coming",
                        Description = "WHY YOU CAME FROM FRONT",
                        Reason = "Meet the Nightmare Apex",
                        Image = "https://raw.githubusercontent.com/Timofey2339/Depth-doors-model/main/Badges/NightmareApexBadge.png"
                    })
                end)
            end
        end)
        task.wait(5)
    end

    if currentTime - lastSpawnTimes.Spread >= 550 then
        lastSpawnTimes.Spread = currentTime
        safeLoad("https://raw.githubusercontent.com/Timofey2339/Depth-doors-model/refs/heads/main/Spread.lua")
        task.delay(10, function()
            if achievementGiver then
                pcall(function()
                    achievementGiver({
                        Title = "Charge",
                        Description = "So much electric!",
                        Reason = "Meet the Spread",
                        Image = "https://raw.githubusercontent.com/Timofey2339/Depth-doors-model/main/Badges/SpreadBadge.png"
                    })
                end)
            end
        end)
        task.wait(5)
    end

    if currentTime - lastSpawnTimes.Greg >= 365 then
        lastSpawnTimes.Greg = currentTime
        safeLoad("https://raw.githubusercontent.com/Timofey2339/Depth-doors-model/main/Greg%20Raw.lua")
        task.delay(4, function()
            if achievementGiver then
                pcall(function()
                    achievementGiver({
                        Title = "Greg Apart",
                        Description = "Why light blinking so long?",
                        Reason = "Survive a Greg Entity",
                        Image = "https://raw.githubusercontent.com/Timofey2339/Depth-doors-model/main/Badges/QregBadgeNew.png"
                    })
                end)
            end
        end)
        task.wait(5)
    end

        if currentTime - lastSpawnTimes.RipperPrime >= 500 then
        lastSpawnTimes.RipperPrime = currentTime
        safeLoad("https://raw.githubusercontent.com/Timofey2339/Depth-doors-model/refs/heads/main/RipperPrime.lua")
        task.delay(4, function()
            if achievementGiver then
                pcall(function()
                    achievementGiver({
                        Title = "Torn Apart",
                        Description = "Why everything is Green?",
                        Reason = "Survive a Ripper Prime Entity",
                        Image = "https://raw.githubusercontent.com/Timofey2339/Depth-doors-model/main/Badges/RipperPrimeBadge.png"
                    })
                end)
            end
        end)
        task.wait(5)
    end

    if currentTime - lastSpawnTimes.Feast >= 425 then
        lastSpawnTimes.Feast = currentTime
        safeLoad("https://raw.githubusercontent.com/Timofey2339/Depth-doors-model/main/Feast.lua")
        task.delay(10.2, function()
            if achievementGiver then
                pcall(function()
                    achievementGiver({
                        Title = "Already Full",
                        Description = "fed up with this",
                        Reason = "Survive the Feast",
                        Image = "http://www.roblox.com/asset/?id=11703907069"
                    })
                end)
            end
        end)
        task.wait(5)
    end

    if currentTime - lastSpawnTimes.Glacher >= 450 then
        lastSpawnTimes.Glacher = currentTime
        safeLoad("https://raw.githubusercontent.com/Timofey2339/Depth-doors-model/main/Glacher.lua")
        task.delay(20, function()
            if achievementGiver then
                pcall(function()
                    achievementGiver({
                        Title = "Boring",
                        Description = "Why so slow?",
                        Reason = "Survive a Glacher Entity",
                        Image = "https://raw.githubusercontent.com/Timofey2339/Depth-doors-model/main/Badges/GlacherBadge.png"
                    })
                end)
            end
        end)
        task.wait(5)
    end

    if currentTime - lastSpawnTimes.A179 >= 550 then
        lastSpawnTimes.A179 = currentTime
        safeLoad("https://raw.githubusercontent.com/Timofey2339/Depth-doors-model/main/A-179%20Raw.lua")
        task.wait(5)
    end

    if currentTime - lastSpawnTimes.Rebound >= 650 then
        lastSpawnTimes.Rebound = currentTime
        safeLoad("https://raw.githubusercontent.com/Timofey2339/Depth-doors-model/main/Rebound%20Raw.lua")
        task.delay(26.2, function()
            if achievementGiver then
                pcall(function()
                    achievementGiver({
                        Title = "Rebounder",
                        Description = "Bruh you going back every door",
                        Reason = "Survive the Rebound",
                        Image = "https://raw.githubusercontent.com/Timofey2339/Depth-doors-model/main/Badges/ReboundBadge.png"
                    })
                end)
            end
        end)
        task.wait(5)
    end

    if currentTime - lastSpawnTimes.Anxiety >= 700 then
        lastSpawnTimes.Anxiety = currentTime
        safeLoad("https://raw.githubusercontent.com/Timofey2339/Depth-doors-model/main/Anxiety.lua")
        task.delay(36.2, function()
            if achievementGiver then
                pcall(function()
                    achievementGiver({
                        Title = "Earthguicker",
                        Description = "WOAH EARTHGUICK STARTED",
                        Reason = "Meet the Anxiety",
                        Image = "https://raw.githubusercontent.com/Timofey2339/Depth-doors-model/main/Badges/AnxietyBadge.png"
                    })
                end)
            end
        end)
        task.wait(5)
    end

    if currentTime - lastSpawnTimes.Dread >= 750 then
        lastSpawnTimes.Dread = currentTime
        safeLoad("https://raw.githubusercontent.com/Timofey2339/Depth-doors-model/main/Fanmade%20Dread.lua")
        task.wait(5)
    end

    if currentTime - lastSpawnTimes.Monoxide >= 800 then
        lastSpawnTimes.Monoxide = currentTime
        safeLoad("https://raw.githubusercontent.com/Timofey2339/Depth-doors-model/main/Monoxide.lua")
        task.delay(13.2, function()
            if achievementGiver then
                pcall(function()
                    achievementGiver({
                        Title = "Crash Out",
                        Description = "i gonna crash your game",
                        Reason = "Survive the Monoxide",
                        Image = "https://raw.githubusercontent.com/Timofey2339/Depth-doors-model/main/Badges/Monoxide.png"
                    })
                end)
            end
        end)
        task.wait(5)
    end

    if currentTime - lastSpawnTimes.Rambey >= 840 then
        lastSpawnTimes.Rambey = currentTime
        safeLoad("https://raw.githubusercontent.com/Timofey2339/Depth-doors-model/main/Rambey%20Raw.lua")
        task.delay(26.2, function()
            if achievementGiver then
                pcall(function()
                    achievementGiver({
                        Title = "What",
                        Description = "Stop rebounding like ambush",
                        Reason = "Survive the Rambey",
                        Image = "https://raw.githubusercontent.com/Timofey2339/Depth-doors-model/main/Badges/RambeyBadge.png"
                    })
                end)
            end
        end)
        task.wait(5)
    end

    if currentTime - lastSpawnTimes.Spark >= 950 then
        lastSpawnTimes.Spark = currentTime
        safeLoad("https://raw.githubusercontent.com/Timofey2339/Depth-doors-model/main/Spark.lua")
        task.delay(26.2, function()
            if achievementGiver then
                pcall(function()
                    achievementGiver({
                        Title = "Sparker",
                        Description = "FASTER FASTER FASTER",
                        Reason = "Survive the Spark",
                        Image = "https://raw.githubusercontent.com/Timofey2339/Depth-doors-model/main/Badges/SparkBadge.png"
                    })
                end)
            end
        end)
        task.wait(5)
    end

    if currentTime - lastSpawnTimes.Wide >= 1050 then
        lastSpawnTimes.Wide = currentTime
        safeLoad("https://raw.githubusercontent.com/Timofey2339/Depth-doors-model/main/Wide.lua")
        task.delay(16.2, function()
            if achievementGiver then
                pcall(function()
                    achievementGiver({
                        Title = "What was that?",
                        Description = "That was so fast",
                        Reason = "Survive the Wide",
                        Image = "https://raw.githubusercontent.com/Timofey2339/Depth-doors-model/main/Badges/WideBadge.png"
                    })
                end)
            end
        end)
        task.wait(5)
    end

    if currentTime - lastSpawnTimes.A60 >= 1200 then
        lastSpawnTimes.A60 = currentTime
        safeLoad("https://raw.githubusercontent.com/Timofey2339/Depth-doors-model/main/A-60%20Raw.lua")
        task.delay(26.2, function()
            if achievementGiver then
                pcall(function()
                    achievementGiver({
                        Title = "WOAH",
                        Description = "What is this scream?",
                        Reason = "Survive the A-60",
                        Image = "https://raw.githubusercontent.com/Timofey2339/Depth-doors-model/main/Badges/A_60_...yes.png"
                    })
                end)
            end
        end)
        task.wait(5)
    end

    isSpawning = false
end)

safeLoad("https://raw.githubusercontent.com/Timofey2339/Depth-doors-model/main/Massage%20Raw.lua")
